/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  2.3.0                                 |
|   \\  /    A nd           | Web:      www.OpenFOAM.org                      |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      fvOptions;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

disk1
{
    type            actuationDiskSource;
    active          on;            //on/off switch
    selectionMode   cellSet;       //cellSet // points //cellZone
    cellSet         actuationDisk1;//cellSet name when selectionMode = cellSet

    actuationDiskSourceCoeffs
    {
        fieldNames  (U);
        diskDir     (1 0 0);   // orientation of the disk
        Cp          0.34;      // Cp
        Ct          0.7;       // Ct
        diskArea    0.0490873852;         // disk area
        upstreamPoint (35 0.5 0.1875);
    }
}

disk2
{
    type            actuationDiskSource;
    active          on;            //on/off switch
    selectionMode   cellSet;       //cellSet // points //cellZone
    cellSet         actuationDisk2;//cellSet name when selectionMode = cellSet

    actuationDiskSourceCoeffs
    {
        fieldNames  (U);
        diskDir     (1 0 0);   // orientation of the disk
        Cp          0.2516;      // Cp
        Ct          0.7;       // Ct
        diskArea    0.0490873852;         // disk area
        upstreamPoint (37 0.5 0.1875);
    }
}

disk3
{
    type            actuationDiskSource;
    active          on;            //on/off switch
    selectionMode   cellSet;       //cellSet // points //cellZone
    cellSet         actuationDisk3;//cellSet name when selectionMode = cellSet

    actuationDiskSourceCoeffs
    {
        fieldNames  (U);
        diskDir     (1 0 0);   // orientation of the disk
        Cp          0.1666;      // Cp
        Ct          0.7;       // Ct
        diskArea    0.0490873852;         // disk area
        upstreamPoint (39 0.5 0.1875);
    }
}

TurbulenceSource1
{
    type scalarExplicitSetValue;
    active true;
    selectionMode cellSet;
    cellSet actuationDisk1;

    scalarExplicitSetValueCoeffs
    {
        volumeMode      absolute; //specific
        injectionRate
        {
            k   2.584;
            epsilon   75;//l = 
        }
    }
}

TurbulenceSource2
{
    type scalarExplicitSetValue;
    active true;
    selectionMode cellSet;
    cellSet actuationDisk2;

    scalarExplicitSetValueCoeffs
    {
        volumeMode      absolute; //specific
        injectionRate
        {
            k   1.993;
            epsilon   50.8;//l = 0.1 mm
        }
    }
}

TurbulenceSource3
{
    type scalarExplicitSetValue;
    active true;
    selectionMode cellSet;
    cellSet actuationDisk3;

    scalarExplicitSetValueCoeffs
    {
        volumeMode      absolute; //specific
        injectionRate
        {
            k   1.3197;
            epsilon   27.398;//l = 0.1 mm
        }
    }
}

//disk2
//{
  //  type            actuationDiskSource;
  //  active          on;
  //  selectionMode   cellSet;
  //  cellSet         actuationDisk2;

//    actuationDiskSourceCoeffs
//    {
//        fieldNames  (U);
//        diskDir     (1 0 0);
//        Cp          0.53;
//        Ct          0.58;
//        diskArea    0.05;
//        upstreamPoint (25 0 0.6);//orig z 1070
//    }
//}

// ************************************************************************* //
